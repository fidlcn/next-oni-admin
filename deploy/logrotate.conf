# logrotate 配置 —— PM2 日志轮转
# 放到 /etc/logrotate.d/next-oni-admin
# 防止日志文件撑爆磁盘

/var/log/next-oni-admin/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
    maxsize 50M
}
